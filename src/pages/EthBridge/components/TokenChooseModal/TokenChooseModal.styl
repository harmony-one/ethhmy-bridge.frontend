@require '../../../../variables.styl'

.layer.layer {
  max-height: 480px;
  border: 1px solid $NGray2;
  background-color: $NBlack3;
  border-radius: 10px;
}

.searchContainer {
  border-bottom: 1px solid $NGray2;
}

.borderBottom {
  border-bottom: 1px solid $NGray2;
}